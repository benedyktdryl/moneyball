export const HEADLESS_MODE = "headless";
export const SKIP_CACHE = "skip";

export const BASE_URL = "https://fantasy.ekstraklasa.org";
export const LOGIN_URL = `${BASE_URL}/login`;
export const STATS_URL = `${BASE_URL}/stats`;

export const MINIMAL_TIME_THRESHOLD = 0.75;

export const POSITIONS_ORDER = { B: 1, O: 2, P: 3, N: 4 };

export const MAX_PLAYERS_FROM_SAME_TEAM = 3;

export const MAX_BUDGET = 30;

export const MAX_PLAYERS_PER_FORMATION = {
  B: 2,
  O: 5,
  P: 5,
  N: 3,
};
